import type { Metadata } from 'next';

export const metadata: Metadata = { title: '매장 등록' };

export default function OwnerNewLayout({ children }: { children: React.ReactNode }) {
  return children;
}
